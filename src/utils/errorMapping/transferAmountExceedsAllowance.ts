import { RevertErrorMap } from "./index";

export const transferAmountExceedsAllowance: RevertErrorMap = {
  identifier: "transfer amount exceeds allowance",
  replacement: "transfer amount exceeds allowance",
};
