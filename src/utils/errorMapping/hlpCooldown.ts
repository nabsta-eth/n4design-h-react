import { RevertErrorMap } from "./index";

export const hlpCooldown: RevertErrorMap = {
  identifier: "HlpManager: cooldown duration not yet",
  replacement: "handle liquidity token (hLP) trading cooldown not expired",
};
