import { RevertErrorMap } from "./index";

export const vaultPriceFeedNewPrice: RevertErrorMap = {
  identifier: "VaultPriceFeed: Must submit new price",
  replacement: "must submit new oracle price",
};
