import { RevertErrorMap } from "./index";

export const transferAmountExceedsBalance: RevertErrorMap = {
  identifier: "transfer amount exceeds balance",
  replacement: "transfer amount exceeds balance",
};
