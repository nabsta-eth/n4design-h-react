type PositionSkewDataProps = {};

// TODO: re-implement for v2.
export const usePositionSkewData = ({}: PositionSkewDataProps) => {
  return {
    longPositionTotal: 0,
    shortPositionTotal: 0,
  };
};
