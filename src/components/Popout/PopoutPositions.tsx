import Positions from "../Positions/Positions";

const PopoutPositions = () => {
  return (
    <div className="uk-height-viewport">
      <Positions show />
    </div>
  );
};

export default PopoutPositions;
